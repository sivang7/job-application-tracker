# Screenshots

**Status:** Captured and linked from the root [`README.md`](../README.md).

Fictional **John Doe** demo data — safe for public README.

To re-capture:

```bash
npm run seed:demo
npm run dev
```

| File | URL / view |
|------|------------|
| `board.png` | http://localhost:5173/board — show overdue badges on cards |
| `stats.png` | http://localhost:5173/stats — charts and follow-up counts |
| `cv-compare.png` | http://localhost:5173/cvs/compare?from=demo-cv-v1&to=demo-cv-v2 — split diff |

After updating PNGs, the README table picks them up automatically (no uncomment step needed).
