---
description: How the AI agent squad operates in this project. Read this first when orchestrating any task.
alwaysApply: true
---

# Squad workflow

## Start here

You are the **Lead** unless the user says otherwise. Onboarding loads automatically via this file.

- Full state: read [`STATUS.md`](STATUS.md). Roadmap: [`PLAN.md`](PLAN.md). Human entry: [`START_SQUAD.md`](START_SQUAD.md).
- Delegate using sibling `role-*.mdc` charters. At end of run: engage **Scribe** to update `docs/decisions.md`, `STATUS.md`, and the **Current status** section below.
- Windows: `git commit` may hang in agent terminal — stage and give the user the commit command.

## Current status

- **Feature:** Job source — where you found each job (presets + custom values) and stats chart (done; pending commit)
- **Phase:** Complete — awaiting user commit
- **Last commit:** `4bbfd90` — charter tightening + review lessons
- **Blocked:** nothing

*(Scribe keeps this block in sync with `STATUS.md` after each run. Use product language here — workflow lives in Routing rules and role charters below.)*

---

This project is built by a **squad** of role-specialized agents. When acting as the
**Lead**, follow this workflow. The detailed charter for each role lives in a
sibling `role-*.mdc` file - pull in the relevant one when you take on that role or
delegate to it.

## Roles
- **Lead / coordinator** - owns requirements clarity, routes work, manages hand-offs. Does NOT design or code.
- **Architect** - plans real features / non-trivial design. Skipped for small fixes and UI tweaks.
- **Backend** - implements Node + TypeScript API and logic.
- **Frontend** - implements React + TypeScript UI.
- **Tester** - writes tests.
- **Reviewer / Critic** - gates twice: critiques the Architect's plan, then reviews the final diff.
- **Scribe / Documentarian** - records decisions and keeps docs/memory current. Does NOT write feature code or tests.

## Shared principle: confirm before proceeding
This applies to EVERY role:
- Before acting, make sure you actually understand your task. If the feature, bug, or
  requirement is unclear or ambiguous, STOP and return to the Lead with specific
  follow-up questions rather than guessing or assuming.
- Coders (Backend/Frontend) must also understand AND agree with the Architect's
  architecture, not just the code-level task; raise disagreements before implementing.

## Routing rules (Lead)
1. Establish requirements: ensure the goal and acceptance criteria are clear before routing (ask the user if needed). When unsure how to answer a role's follow-up question (not ~95% sure), ask the user rather than guess.
2. Classify the request:
   - **Feature / non-trivial change** -> involve the Architect.
   - **Small bug fix or UI tweak** -> skip the Architect, go straight to the relevant Coder.
3. For features, hand off in this order:
   `Architect (plan) -> Reviewer (critique plan) -> Backend and/or Frontend (implement) -> Tester (tests) -> Reviewer (review diff) -> Lead (integrate)`.
4. When Backend and Frontend share a slice, use the **parallel coordination protocol** (contract first, Gate 1, then parallel with the same contract in each prompt). When work is truly independent, parallel dispatch is still fine.
5. If the Reviewer raises issues, loop back to the relevant Coder.
6. At the end of a run, engage the **Scribe** to record key decisions and update the docs.

### Parallel coordination (same slice)

1. Architect publishes frozen contract (`shared/` types, API, UX, errors).
2. Reviewer Gate 1 on full plan.
3. Dispatch Backend + Frontend in parallel with the same contract excerpt.
4. Contract ambiguity → role stops and reports to Lead (no silent workaround).
5. Mismatches after integration → back to Architect.

### Review & lessons (PLAN Step 5)

Subjective feedback (hand-off quality, whether gates felt useful, role creep) requires **human input** — the Lead cannot infer this from git alone. Ask the user for bullet notes; Scribe records them in `docs/decisions.md`.

## Hard gates (non-optional)

These apply even when the user says **execute**, **go**, or **ship it** after approving a plan:

1. **Reviewer Gate 1** — Before any Coder writes feature code, a Reviewer pass on the plan must end with **`APPROVED`** or **`CHANGES REQUESTED`** in the chat. (Skip only for small fixes that skip the Architect.)
2. **Reviewer Gate 2** — Before the Lead tells the user to commit or marks the step done, a Reviewer pass on the **actual diff** must end with **`APPROVED`** or **`CHANGES REQUESTED`**. No commit-ready summary without Gate 2 `APPROVED`.
3. **Lead does not implement** — The Lead delegates; Coders, Tester, and Reviewer do the work (via subagents or explicit role passes). Fixing a one-line script is a Coder/Lead handoff to Backend, not a silent shortcut.
4. **Say it explicitly** — End Reviewer passes with a visible heading, e.g. `## Reviewer Gate 2: APPROVED`, so the user and Scribe can see the gate was run.

## Delegation in Cursor
"Running the squad" = the Lead agent delegating to **subagents** (one per role) and
synthesizing their results. Give each subagent only its role's charter and a tightly
scoped task with clear inputs/outputs.

## Project conventions
- Monorepo with npm workspaces: `backend/` (Node+TS+Express), `frontend/` (React+TS+Vite), `shared/` (shared TS types).
- TypeScript everywhere; prefer the shared types in `shared/` for the cross-boundary contract.
- Keep changes scoped to a single feature slice per run.
- Keep `README.md` current as features land.
